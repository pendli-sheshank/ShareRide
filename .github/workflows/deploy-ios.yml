# iOS build is planned for a future milestone.
# KMP shared module is set up; add iosX64/iosArm64/iosSimulatorArm64 targets
# to shared/build.gradle.kts and create iosApp/ Xcode project to enable this.
name: Build iOS (KMP) — placeholder

on:
  workflow_dispatch: {}   # manual trigger only until iOS target is ready

jobs:
  placeholder:
    runs-on: macos-latest
    steps:
      - run: echo "iOS build not yet configured. See shared/build.gradle.kts."
